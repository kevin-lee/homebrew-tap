# Kevin-lee Tap

## How do I install these formulae?

`brew install kevin-lee/tap/<formula>`

Or `brew tap kevin-lee/tap` and then `brew install <formula>`.

Or, in a `brew bundle` `Brewfile`:

```ruby
tap "kevin-lee/tap"
brew "<formula>"
```

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
